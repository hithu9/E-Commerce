<!DOCTYPE html>
<html>
<head>
  <title>VogueMart</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="icon" type="image/png" href="/images/logo.png">

</head>

<body style="background-color:#214341;">
    <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
    <div class="container">
      <a class="navbar-brand d-flex align-items-center" href="/">
  <img src="/images/logo.png" alt="VogueMart Logo" style="height:40px; width:auto; margin-right:10px;">
  <span>VogueMart</span>
</a>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="mainNavbar">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-lg-center">
          <li class="nav-item">
            <a class="nav-link" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/products">Shop</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/cart">Cart</a>
          </li>
          <% if (user) { %>
  <li class="nav-item">
    <a href="/profile" class="nav-link">Profile</a>
  </li>
  <li class="nav-item">
    <a href="/logout" class="btn btn-outline-light ms-lg-2">Logout</a>
  </li>
<% } else { %>
  <li class="nav-item">
    <a href="/login" class="btn btn-outline-light ms-lg-2">Login</a>
  </li>
<% } %>

        </ul>
      </div>
    </div>
  </nav>
<div class="container py-5">

  <h2 class="text-light mb-4">Checkout</h2>

  <!-- Display message if order placed -->
  <% if (message) { %>
    <div class="alert alert-success text-center">
      <%= message %>
    </div>
  <% } %>

  <% if (product) { %>
    <div class="card p-4 shadow-sm" style="background-color:#b2ced5;">
      <h4><%= product.name %></h4>
      <p class="text-success">₹ <%= product.price %></p>
      
      <form action="/place-order" method="POST">
        <label class="mt-3">Full Name</label>
        <input type="text" name="fullname" class="form-control" required>

        <label class="mt-3">Address</label>
        <textarea name="address" class="form-control" required></textarea>

        <label class="mt-3">Phone</label>
        <input type="text" name="phone" class="form-control" required>

        <button class="btn btn-success w-100 mt-4">Place Order</button>
      </form>
    </div>
  <% } else { %>
    <p class="text-light">No product selected.</p>
  <% } %>

  <a href="/cart" class="btn btn-secondary mt-3">Cancel</a>
</div>
  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"
  ></script>
</body>
</html>
